require 'rails_helper'

RSpec.describe User, type: :model do
  context "with social auth" do
    it "can auth with facebook" do
    end
    it "can auth with vk" do
    end
    it "can auth with ok" do
    end
    it "can auth with instagram" do
    end
  end
end
